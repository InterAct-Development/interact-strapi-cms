'use strict';

/**
 * action-translation service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::action-translation.action-translation');
