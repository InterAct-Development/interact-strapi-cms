'use strict';

/**
 * pretest-questionnaire-page service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::pretest-questionnaire-page.pretest-questionnaire-page');
